# rehistory

Rewrite the entire default branch commit history with synthesized messages

Install and run:

```bash
chmod +x rehistory.mjs
npm link

rehistory OWNER/REPO --dry-run --keep-temp
rehistory OWNER/REPO --yes
```
